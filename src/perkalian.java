public class perkalian {
    public static void tugas_no_5(int base, int start, int loop) {
    

        for (int i = 0; i < loop; i++) {

            int result = base * (start + i);

            System.out.println(base + " x " + (start + i) + " = " + result);
        }
    }
}
    
